import tensorflow as tf
import numpy as np

import Seq2SeqModel 
import tokenizer
#implementing seq2seq

def main():
    
    #not completed yet
    
    #suppose we re given data with buckets
    
    
    
    mod=Model(source_vocab_size, target_vocab_size, buckets, size, num_layers)
    
    
    batch1=mod.get_batch(data, 3)
    
    
    
    
